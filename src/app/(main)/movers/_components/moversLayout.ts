/**
 * `/movers` 목록에서 반복되는 패딩·셸 클래스.
 * 일회성 클래스는 각 파일 JSX에 그대로 둔다.
 */

/**
 * [리팩터][1][신규] 목록 타이틀·본문 좌우 패딩.
 * 문자열은 리팩터 전 `pageXPadding`과 동일. Mobile 24px / Tablet 72px / Desktop 40·64·260
 */
export const MOVERS_PAGE_X_PADDING =
  'px-6 md:px-[4.5rem] lg:px-10 xl:px-16 min-[90rem]:px-[16.25rem]';
